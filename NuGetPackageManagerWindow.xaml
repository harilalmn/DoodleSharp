<Window x:Class="DoodleSharp.NuGetPackageManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage NuGet Packages" Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E">
    
    <Grid Margin="10">
        <TabControl Background="#2D2D30" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Foreground" Value="#CCCCCC"/>
                    <Setter Property="Background" Value="#1E1E1E"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="#3E3E42" CornerRadius="4,4,0,0" Margin="2,0">
                                    <ContentPresenter x:Name="ContentSite"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Center"
                                        ContentSource="Header"
                                        Margin="10,5"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#252526" />
                                        <Setter Property="Foreground" Value="White" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="#1E1E1E" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <Style x:Key="HyperlinkButtonStyle" TargetType="Button">
                    <Setter Property="Foreground" Value="#007ACC"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}">
                                    <TextBlock Text="{TemplateBinding Content}" 
                                               TextDecorations="Underline"
                                               Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="#005A9E"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <!-- Installed Tab -->
            <TabItem Header="Installed">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListView x:Name="PackagesList" Grid.Row="0" Background="#252526" BorderBrush="#3E3E42" Foreground="#E0E0E0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Package ID" Width="300" DisplayMemberBinding="{Binding Id}"/>
                                <GridViewColumn Header="Version" Width="150" DisplayMemberBinding="{Binding Version}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <Button Content="Remove Selected" Grid.Row="1" HorizontalAlignment="Right" Margin="0,10,0,0" Click="RemoveButton_Click" Padding="10,5"/>
                </Grid>
            </TabItem>

            <!-- Browse Tab -->
            <TabItem Header="Browse">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Search Bar -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SearchBox" Grid.Column="0" Margin="0,0,10,0" Padding="5" KeyDown="SearchBox_KeyDown" ToolTip="Search NuGet..."/>
                        <Button Content="Search" Grid.Column="1" Padding="15,5" Click="SearchButton_Click"/>
                    </Grid>

                    <!-- Results List -->
                    <ListView x:Name="SearchResultsList" Grid.Row="1" Background="#252526" BorderBrush="#3E3E42" Foreground="#E0E0E0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="5,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                        <TextBlock Text="{Binding Id}" FontWeight="Bold" FontSize="14" Foreground="#569CD6"/>
                                        <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#CCCCCC" TextTrimming="CharacterEllipsis" MaxHeight="32" Margin="0,2,0,5" TextWrapping="Wrap"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="Version:" Foreground="#969696" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                            <ComboBox ItemsSource="{Binding Versions}" 
                                                      SelectedItem="{Binding SelectedVersion}" 
                                                      Width="120" 
                                                      DropDownOpened="VersionComboBox_DropDownOpened"
                                                      Tag="{Binding}"
                                                      VerticalAlignment="Center">
                                            </ComboBox>
                                        </StackPanel>
                                    </StackPanel>
                                    <Button Grid.Column="1" 
                                            Content="Install" 
                                            Style="{StaticResource HyperlinkButtonStyle}"
                                            VerticalAlignment="Center" 
                                            HorizontalAlignment="Right"
                                            Click="InstallButton_Click" 
                                            Tag="{Binding}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    
                    <ProgressBar x:Name="SearchProgressBar" Grid.Row="2" Height="4" IsIndeterminate="True" Visibility="Collapsed" Margin="0,5,0,0"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
