<Window x:Class="DoodleSharp.AddReferenceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Reference"
        Width="500" Height="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource PrimaryBackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tab Control -->
        <TabControl Grid.Row="1" 
                    Background="{StaticResource SecondaryBackgroundBrush}"
                    BorderBrush="{StaticResource BorderBrush}">
            
            <!-- Framework Tab -->
            <TabItem Header="Framework" Foreground="{StaticResource ForegroundBrush}">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="Select .NET framework assemblies to reference:"
                               Foreground="{StaticResource ForegroundBrush}"
                               Margin="0,0,0,8"/>
                    
                    <ListBox Grid.Row="1" 
                             x:Name="FrameworkListBox"
                             Background="{StaticResource PrimaryBackgroundBrush}"
                             Foreground="{StaticResource ForegroundBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             SelectionMode="Multiple">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding Name}"
                                          IsChecked="{Binding IsSelected}"
                                          Foreground="{StaticResource ForegroundBrush}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
            
            <!-- Browse Tab -->
            <TabItem Header="Browse" Foreground="{StaticResource ForegroundBrush}">
                <Grid Margin="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                               Text="Add local DLL assemblies:"
                               Foreground="{StaticResource ForegroundBrush}"
                               Margin="0,0,0,8"/>
                    
                    <Button Grid.Row="1" 
                            Content="Browse for DLL..."
                            Click="BrowseDll_Click"
                            Style="{StaticResource RibbonButtonStyle}"
                            Foreground="{StaticResource ForegroundBrush}"
                            HorizontalAlignment="Left"
                            Padding="16,8"
                            Margin="0,0,0,8"/>
                    
                    <ListBox Grid.Row="2" 
                             x:Name="LocalDllsListBox"
                             Background="{StaticResource PrimaryBackgroundBrush}"
                             Foreground="{StaticResource ForegroundBrush}"
                             BorderBrush="{StaticResource BorderBrush}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding}" 
                                               Foreground="{StaticResource ForegroundBrush}"
                                               VerticalAlignment="Center"/>
                                    <Button Grid.Column="1" Content="✕" 
                                            Tag="{Binding}"
                                            Click="RemoveDll_Click"
                                            Background="Transparent"
                                            Foreground="{StaticResource ErrorBrush}"
                                            BorderThickness="0"
                                            Padding="8,2"
                                            Cursor="Hand"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="OK" 
                    Width="80" 
                    Click="OkButton_Click"
                    Style="{StaticResource RunButtonStyle}"
                    Foreground="White"
                    Margin="0,0,8,0"/>
            <Button Content="Cancel" 
                    Width="80" 
                    Click="CancelButton_Click"
                    Style="{StaticResource RibbonButtonStyle}"
                    Foreground="{StaticResource ForegroundBrush}"/>
        </StackPanel>
    </Grid>
</Window>
